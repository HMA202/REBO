# شرح التمرين `45_string_to_float_simple` — string to float simple

## فكرة المجلد

هذا تمرين تدريبي إضافي. ملف `exercise.go` يحتوي الحل أو الدوال الأساسية، وملف `main.go` يحتوي أمثلة تشغيل تساعد على رؤية النتيجة.

## الدوال الموجودة

- `StringToFloatSimple(s string) float64`

## `exercise.go`

```go
package main

func StringToFloatSimple(s string) float64 {
	if s == "" {
		return 0
	}

	sign := 1.0
	index := 0

	if s[0] == '-' {
		sign = -1
		index = 1
	}

	result := 0.0
	decimal := false
	divisor := 10.0

	for index < len(s) {
		ch := s[index]

		if ch == '.' && !decimal {
			decimal = true
			index++
			continue
		}

		if ch < '0' || ch > '9' {
			return 0
		}

		digit := float64(ch - '0')

		if !decimal {
			result = result*10 + digit
		} else {
			result += digit / divisor
			divisor *= 10
		}

		index++
	}

	return result * sign
}
```

### شرح كل سطر في الحل

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func StringToFloatSimple(s string) float64 {` | تعريف دالة `StringToFloatSimple` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `if s == "" {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 5 | `return 0` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 6 | `}` | نهاية الكتلة الحالية. |
| 8 | `sign := 1.0` | إنشاء المتغير `sign` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 9 | `index := 0` | إنشاء المتغير `index` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 11 | `if s[0] == '-' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 12 | `sign = -1` | إسناد أو تحديث قيمة `sign` وفق التعبير الموجود في الجهة اليمنى. |
| 13 | `index = 1` | إسناد أو تحديث قيمة `index` وفق التعبير الموجود في الجهة اليمنى. |
| 14 | `}` | نهاية الكتلة الحالية. |
| 16 | `result := 0.0` | إنشاء المتغير `result` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 17 | `decimal := false` | إنشاء المتغير `decimal` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 18 | `divisor := 10.0` | إنشاء المتغير `divisor` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 20 | `for index < len(s) {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 21 | `ch := s[index]` | إنشاء المتغير `ch` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 23 | `if ch == '.' && !decimal {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 24 | `decimal = true` | إسناد أو تحديث قيمة `decimal` وفق التعبير الموجود في الجهة اليمنى. |
| 25 | `index++` | يزيد قيمة العداد بمقدار 1. |
| 26 | `continue` | يتجاوز بقية أوامر الدورة الحالية وينتقل إلى الدورة التالية من الحلقة. |
| 27 | `}` | نهاية الكتلة الحالية. |
| 29 | `if ch < '0' \|\| ch > '9' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 30 | `return 0` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 31 | `}` | نهاية الكتلة الحالية. |
| 33 | `digit := float64(ch - '0')` | إنشاء المتغير `digit` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 35 | `if !decimal {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 36 | `result = result*10 + digit` | إسناد أو تحديث قيمة `result` وفق التعبير الموجود في الجهة اليمنى. |
| 37 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 38 | `result += digit / divisor` | إسناد أو تحديث قيمة `result +` وفق التعبير الموجود في الجهة اليمنى. |
| 39 | `divisor *= 10` | إسناد أو تحديث قيمة `divisor *` وفق التعبير الموجود في الجهة اليمنى. |
| 40 | `}` | نهاية الكتلة الحالية. |
| 42 | `index++` | يزيد قيمة العداد بمقدار 1. |
| 43 | `}` | نهاية الكتلة الحالية. |
| 45 | `return result * sign` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 46 | `}` | نهاية الكتلة الحالية. |

## كيف يعمل الحل؟

- يستخدم حلقة `for` لتكرار الفحص أو المعالجة.
- يحتوي استدعاءات دوال؛ راقب هل يوجد recursion أو دوال مساعدة.
- قد يستخدم مؤشرات أو عمليات ضرب؛ يُحدد المعنى من السياق والتوقيع.

### تتبع عام للتنفيذ

1. يبدأ التنفيذ عند استدعاء الدالة الأساسية بالقيم المطلوبة.
2. تُنشأ المتغيرات المساعدة والقيم الابتدائية.
3. تنفذ الحلقات أو الشروط بالترتيب الظاهر في الكود.
4. عند تحقق حالة إرجاع مبكر تتوقف الدالة فورًا؛ وإلا تستمر حتى النتيجة النهائية.
5. القيمة المعادة أو التعديل النهائي يظهر في `main.go`.

## `main.go` المستخدم للتجربة

```go
package main

import "fmt"

func main() {
	fmt.Println(StringToFloatSimple("12.5"))
	fmt.Println(StringToFloatSimple("-3.14"))
	fmt.Println(StringToFloatSimple("abc"))
}
```

### شرح أسطر ملف التجربة

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "fmt"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `fmt.Println(StringToFloatSimple("12.5"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 7 | `fmt.Println(StringToFloatSimple("-3.14"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 8 | `fmt.Println(StringToFloatSimple("abc"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 9 | `}` | نهاية الكتلة الحالية. |

## طريقة التشغيل

```bash
cd test/exercisestests/45_string_to_float_simple
go run .
```

## نصائح لفهم التمرين

- اكتب قيم المتغيرات على ورقة بعد كل دورة من الحلقة.
- فرّق بين index الذي يبدأ غالبًا من 0، وبين العد البشري الذي يبدأ من 1.
- اختبر المدخل الفارغ، والصفر، والقيم السالبة، وعنصرًا واحدًا عندما تكون مناسبة لنوع التمرين.
- لا تحفظ الناتج فقط؛ تتبع سبب تغير كل متغير.

## التعقيد

استخرج التعقيد من عدد الحلقات وحجم البيانات: حلقة واحدة غالبًا `O(n)`، حلقتان متداخلتان غالبًا `O(n²)`، والبحث الثنائي غالبًا `O(log n)`. راجع الكود الفعلي لأن بعض الحلقات تعمل على أبعاد مختلفة.
