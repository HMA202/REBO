# شرح التمرين `49_maze_shortest_path` — maze shortest path

## فكرة المجلد

هذا تمرين تدريبي إضافي. ملف `exercise.go` يحتوي الحل أو الدوال الأساسية، وملف `main.go` يحتوي أمثلة تشغيل تساعد على رؤية النتيجة.

## الدوال الموجودة

- `MazeShortestPath(maze []string) int`

## `exercise.go`

```go
package main

type point struct {
	row  int
	col  int
	dist int
}

func MazeShortestPath(maze []string) int {
	if len(maze) == 0 {
		return -1
	}

	rows := len(maze)
	cols := len(maze[0])

	start := point{-1, -1, 0}

	for r := 0; r < rows; r++ {
		for c := 0; c < cols; c++ {
			if maze[r][c] == 'S' {
				start = point{r, c, 0}
			}
		}
	}

	if start.row == -1 {
		return -1
	}

	visited := make([][]bool, rows)
	for i := 0; i < rows; i++ {
		visited[i] = make([]bool, cols)
	}

	queue := []point{start}
	visited[start.row][start.col] = true

	directions := [][2]int{
		{1, 0},
		{-1, 0},
		{0, 1},
		{0, -1},
	}

	for len(queue) > 0 {
		current := queue[0]
		queue = queue[1:]

		if maze[current.row][current.col] == 'E' {
			return current.dist
		}

		for _, d := range directions {
			nr := current.row + d[0]
			nc := current.col + d[1]

			if nr >= 0 && nr < rows && nc >= 0 && nc < cols {
				if !visited[nr][nc] && maze[nr][nc] != '#' {
					visited[nr][nc] = true
					queue = append(queue, point{nr, nc, current.dist + 1})
				}
			}
		}
	}

	return -1
}
```

### شرح كل سطر في الحل

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `type point struct {` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 4 | `row  int` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 5 | `col  int` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 6 | `dist int` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 7 | `}` | نهاية الكتلة الحالية. |
| 9 | `func MazeShortestPath(maze []string) int {` | تعريف دالة `MazeShortestPath` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 10 | `if len(maze) == 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 11 | `return -1` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 12 | `}` | نهاية الكتلة الحالية. |
| 14 | `rows := len(maze)` | إنشاء المتغير `rows` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 15 | `cols := len(maze[0])` | إنشاء المتغير `cols` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 17 | `start := point{-1, -1, 0}` | إنشاء المتغير `start` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 19 | `for r := 0; r < rows; r++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 20 | `for c := 0; c < cols; c++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 21 | `if maze[r][c] == 'S' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 22 | `start = point{r, c, 0}` | إسناد أو تحديث قيمة `start` وفق التعبير الموجود في الجهة اليمنى. |
| 23 | `}` | نهاية الكتلة الحالية. |
| 24 | `}` | نهاية الكتلة الحالية. |
| 25 | `}` | نهاية الكتلة الحالية. |
| 27 | `if start.row == -1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 28 | `return -1` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 29 | `}` | نهاية الكتلة الحالية. |
| 31 | `visited := make([][]bool, rows)` | ينشئ slice أو بنية ديناميكية بالحجم المطلوب ويخزنها في متغير جديد. |
| 32 | `for i := 0; i < rows; i++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 33 | `visited[i] = make([]bool, cols)` | إسناد أو تحديث قيمة `visited[i]` وفق التعبير الموجود في الجهة اليمنى. |
| 34 | `}` | نهاية الكتلة الحالية. |
| 36 | `queue := []point{start}` | إنشاء المتغير `queue` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 37 | `visited[start.row][start.col] = true` | إسناد أو تحديث قيمة `visited[start.row][start.col]` وفق التعبير الموجود في الجهة اليمنى. |
| 39 | `directions := [][2]int{` | إنشاء المتغير `directions` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 40 | `{1, 0},` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 41 | `{-1, 0},` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 42 | `{0, 1},` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 43 | `{0, -1},` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 44 | `}` | نهاية الكتلة الحالية. |
| 46 | `for len(queue) > 0 {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 47 | `current := queue[0]` | إنشاء المتغير `current` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 48 | `queue = queue[1:]` | إسناد أو تحديث قيمة `queue` وفق التعبير الموجود في الجهة اليمنى. |
| 50 | `if maze[current.row][current.col] == 'E' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 51 | `return current.dist` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 52 | `}` | نهاية الكتلة الحالية. |
| 54 | `for _, d := range directions {` | حلقة `range` تمر على عناصر النص أو الـslice؛ المتغير `_` يعني تجاهل قيمة لا نحتاجها. |
| 55 | `nr := current.row + d[0]` | إنشاء المتغير `nr` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 56 | `nc := current.col + d[1]` | إنشاء المتغير `nc` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 58 | `if nr >= 0 && nr < rows && nc >= 0 && nc < cols {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 59 | `if !visited[nr][nc] && maze[nr][nc] != '#' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 60 | `visited[nr][nc] = true` | إسناد أو تحديث قيمة `visited[nr][nc]` وفق التعبير الموجود في الجهة اليمنى. |
| 61 | `queue = append(queue, point{nr, nc, current.dist + 1})` | يضيف عنصرًا جديدًا إلى نهاية الـslice ويعيد الـslice بعد التحديث. |
| 62 | `}` | نهاية الكتلة الحالية. |
| 63 | `}` | نهاية الكتلة الحالية. |
| 64 | `}` | نهاية الكتلة الحالية. |
| 65 | `}` | نهاية الكتلة الحالية. |
| 67 | `return -1` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 68 | `}` | نهاية الكتلة الحالية. |

## كيف يعمل الحل؟

- يستخدم حلقة `for` لتكرار الفحص أو المعالجة.
- يستخدم `range` للمرور على عناصر slice أو أحرف string.
- يحتوي استدعاءات دوال؛ راقب هل يوجد recursion أو دوال مساعدة.
- يتعامل مع مصفوفة ثنائية الأبعاد، لذلك يجب الانتباه للصف والعمود.
- يستخدم `append` لبناء slice ديناميكيًا.
- قد يستخدم مؤشرات أو عمليات ضرب؛ يُحدد المعنى من السياق والتوقيع.

### تتبع عام للتنفيذ

1. يبدأ التنفيذ عند استدعاء الدالة الأساسية بالقيم المطلوبة.
2. تُنشأ المتغيرات المساعدة والقيم الابتدائية.
3. تنفذ الحلقات أو الشروط بالترتيب الظاهر في الكود.
4. عند تحقق حالة إرجاع مبكر تتوقف الدالة فورًا؛ وإلا تستمر حتى النتيجة النهائية.
5. القيمة المعادة أو التعديل النهائي يظهر في `main.go`.

## `main.go` المستخدم للتجربة

```go
package main

import "fmt"

func main() {
	maze := []string{
		"S..#",
		".#..",
		"...E",
	}

	fmt.Println(MazeShortestPath(maze))
}
```

### شرح أسطر ملف التجربة

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "fmt"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `maze := []string{` | إنشاء المتغير `maze` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 7 | `"S..#",` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 8 | `".#..",` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 9 | `"...E",` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 10 | `}` | نهاية الكتلة الحالية. |
| 12 | `fmt.Println(MazeShortestPath(maze))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 13 | `}` | نهاية الكتلة الحالية. |

## طريقة التشغيل

```bash
cd test/exercisestests/49_maze_shortest_path
go run .
```

## نصائح لفهم التمرين

- اكتب قيم المتغيرات على ورقة بعد كل دورة من الحلقة.
- فرّق بين index الذي يبدأ غالبًا من 0، وبين العد البشري الذي يبدأ من 1.
- اختبر المدخل الفارغ، والصفر، والقيم السالبة، وعنصرًا واحدًا عندما تكون مناسبة لنوع التمرين.
- لا تحفظ الناتج فقط؛ تتبع سبب تغير كل متغير.

## التعقيد

استخرج التعقيد من عدد الحلقات وحجم البيانات: حلقة واحدة غالبًا `O(n)`، حلقتان متداخلتان غالبًا `O(n²)`، والبحث الثنائي غالبًا `O(log n)`. راجع الكود الفعلي لأن بعض الحلقات تعمل على أبعاد مختلفة.
