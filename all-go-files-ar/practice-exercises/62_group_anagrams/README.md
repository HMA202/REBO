# شرح التمرين `62_group_anagrams` — group anagrams

## فكرة المجلد

هذا تمرين تدريبي إضافي. ملف `exercise.go` يحتوي الحل أو الدوال الأساسية، وملف `main.go` يحتوي أمثلة تشغيل تساعد على رؤية النتيجة.

## الدوال الموجودة

- `anagramKey(word string) string`
- `GroupAnagrams(words []string) map[string][]string`

## `exercise.go`

```go
package main

func anagramKey(word string) string {
	counts := [26]int{}

	for _, ch := range word {
		if ch >= 'a' && ch <= 'z' {
			counts[ch-'a']++
		}
	}

	key := ""

	for i := 0; i < 26; i++ {
		key += string(rune('a' + i))
		key += string(rune('0' + counts[i]))
	}

	return key
}

func GroupAnagrams(words []string) map[string][]string {
	groups := map[string][]string{}

	for _, word := range words {
		key := anagramKey(word)
		groups[key] = append(groups[key], word)
	}

	return groups
}
```

### شرح كل سطر في الحل

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func anagramKey(word string) string {` | تعريف دالة `anagramKey` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `counts := [26]int{}` | إنشاء المتغير `counts` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 6 | `for _, ch := range word {` | حلقة `range` تمر على عناصر النص أو الـslice؛ المتغير `_` يعني تجاهل قيمة لا نحتاجها. |
| 7 | `if ch >= 'a' && ch <= 'z' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 8 | `counts[ch-'a']++` | زيادة العداد بمقدار واحد بعد الدورة الحالية. |
| 9 | `}` | نهاية الكتلة الحالية. |
| 10 | `}` | نهاية الكتلة الحالية. |
| 12 | `key := ""` | إنشاء المتغير `key` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 14 | `for i := 0; i < 26; i++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 15 | `key += string(rune('a' + i))` | إسناد أو تحديث قيمة `key +` وفق التعبير الموجود في الجهة اليمنى. |
| 16 | `key += string(rune('0' + counts[i]))` | إسناد أو تحديث قيمة `key +` وفق التعبير الموجود في الجهة اليمنى. |
| 17 | `}` | نهاية الكتلة الحالية. |
| 19 | `return key` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 20 | `}` | نهاية الكتلة الحالية. |
| 22 | `func GroupAnagrams(words []string) map[string][]string {` | تعريف دالة `GroupAnagrams` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 23 | `groups := map[string][]string{}` | إنشاء المتغير `groups` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 25 | `for _, word := range words {` | حلقة `range` تمر على عناصر النص أو الـslice؛ المتغير `_` يعني تجاهل قيمة لا نحتاجها. |
| 26 | `key := anagramKey(word)` | إنشاء المتغير `key` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 27 | `groups[key] = append(groups[key], word)` | يضيف عنصرًا جديدًا إلى نهاية الـslice ويعيد الـslice بعد التحديث. |
| 28 | `}` | نهاية الكتلة الحالية. |
| 30 | `return groups` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 31 | `}` | نهاية الكتلة الحالية. |

## كيف يعمل الحل؟

- يستخدم حلقة `for` لتكرار الفحص أو المعالجة.
- يستخدم `range` للمرور على عناصر slice أو أحرف string.
- يحتوي استدعاءات دوال؛ راقب هل يوجد recursion أو دوال مساعدة.
- يستخدم `map` لتخزين علاقات مفتاح/قيمة أو حساب التكرارات بسرعة.
- يستخدم `append` لبناء slice ديناميكيًا.
- قد يستخدم مؤشرات أو عمليات ضرب؛ يُحدد المعنى من السياق والتوقيع.

### تتبع عام للتنفيذ

1. يبدأ التنفيذ عند استدعاء الدالة الأساسية بالقيم المطلوبة.
2. تُنشأ المتغيرات المساعدة والقيم الابتدائية.
3. تنفذ الحلقات أو الشروط بالترتيب الظاهر في الكود.
4. عند تحقق حالة إرجاع مبكر تتوقف الدالة فورًا؛ وإلا تستمر حتى النتيجة النهائية.
5. القيمة المعادة أو التعديل النهائي يظهر في `main.go`.

## `main.go` المستخدم للتجربة

```go
package main

import "fmt"

func main() {
	groups := GroupAnagrams([]string{"eat", "tea", "tan", "ate", "nat", "bat"})

	for _, group := range groups {
		if len(group) > 1 {
			fmt.Println(group)
		}
	}
}
```

### شرح أسطر ملف التجربة

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "fmt"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `groups := GroupAnagrams([]string{"eat", "tea", "tan", "ate", "nat", "bat"})` | إنشاء المتغير `groups` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 8 | `for _, group := range groups {` | حلقة `range` تمر على عناصر النص أو الـslice؛ المتغير `_` يعني تجاهل قيمة لا نحتاجها. |
| 9 | `if len(group) > 1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 10 | `fmt.Println(group)` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 11 | `}` | نهاية الكتلة الحالية. |
| 12 | `}` | نهاية الكتلة الحالية. |
| 13 | `}` | نهاية الكتلة الحالية. |

## طريقة التشغيل

```bash
cd test/exercisestests/62_group_anagrams
go run .
```

## نصائح لفهم التمرين

- اكتب قيم المتغيرات على ورقة بعد كل دورة من الحلقة.
- فرّق بين index الذي يبدأ غالبًا من 0، وبين العد البشري الذي يبدأ من 1.
- اختبر المدخل الفارغ، والصفر، والقيم السالبة، وعنصرًا واحدًا عندما تكون مناسبة لنوع التمرين.
- لا تحفظ الناتج فقط؛ تتبع سبب تغير كل متغير.

## التعقيد

استخرج التعقيد من عدد الحلقات وحجم البيانات: حلقة واحدة غالبًا `O(n)`، حلقتان متداخلتان غالبًا `O(n²)`، والبحث الثنائي غالبًا `O(log n)`. راجع الكود الفعلي لأن بعض الحلقات تعمل على أبعاد مختلفة.
