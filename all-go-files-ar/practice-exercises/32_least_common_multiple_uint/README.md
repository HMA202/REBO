# شرح التمرين `32_least_common_multiple_uint` — least common multiple uint

## فكرة المجلد

هذا تمرين تدريبي إضافي. ملف `exercise.go` يحتوي الحل أو الدوال الأساسية، وملف `main.go` يحتوي أمثلة تشغيل تساعد على رؤية النتيجة.

## الدوال الموجودة

- `gcdForLCM(a, b uint) uint`
- `LeastCommonMultiple(a, b uint) uint`

## `exercise.go`

```go
package main

func gcdForLCM(a, b uint) uint {
	for b != 0 {
		a, b = b, a%b
	}
	return a
}

func LeastCommonMultiple(a, b uint) uint {
	if a == 0 || b == 0 {
		return 0
	}

	return (a / gcdForLCM(a, b)) * b
}
```

### شرح كل سطر في الحل

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func gcdForLCM(a, b uint) uint {` | تعريف دالة `gcdForLCM` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `for b != 0 {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 5 | `a, b = b, a%b` | إسناد أو تحديث قيمة `a, b` وفق التعبير الموجود في الجهة اليمنى. |
| 6 | `}` | نهاية الكتلة الحالية. |
| 7 | `return a` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 8 | `}` | نهاية الكتلة الحالية. |
| 10 | `func LeastCommonMultiple(a, b uint) uint {` | تعريف دالة `LeastCommonMultiple` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 11 | `if a == 0 \|\| b == 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 12 | `return 0` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 13 | `}` | نهاية الكتلة الحالية. |
| 15 | `return (a / gcdForLCM(a, b)) * b` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 16 | `}` | نهاية الكتلة الحالية. |

## كيف يعمل الحل؟

- يستخدم حلقة `for` لتكرار الفحص أو المعالجة.
- يحتوي استدعاءات دوال؛ راقب هل يوجد recursion أو دوال مساعدة.
- عامل `%` يُستخدم للباقي، وغالبًا لفحص القابلية للقسمة أو استخراج الخانات.
- قد يستخدم مؤشرات أو عمليات ضرب؛ يُحدد المعنى من السياق والتوقيع.

### تتبع عام للتنفيذ

1. يبدأ التنفيذ عند استدعاء الدالة الأساسية بالقيم المطلوبة.
2. تُنشأ المتغيرات المساعدة والقيم الابتدائية.
3. تنفذ الحلقات أو الشروط بالترتيب الظاهر في الكود.
4. عند تحقق حالة إرجاع مبكر تتوقف الدالة فورًا؛ وإلا تستمر حتى النتيجة النهائية.
5. القيمة المعادة أو التعديل النهائي يظهر في `main.go`.

## `main.go` المستخدم للتجربة

```go
package main

import "fmt"

func main() {
	fmt.Println(LeastCommonMultiple(47, 11))
	fmt.Println(LeastCommonMultiple(6, 8))
	fmt.Println(LeastCommonMultiple(12, 18))
}
```

### شرح أسطر ملف التجربة

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "fmt"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `fmt.Println(LeastCommonMultiple(47, 11))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 7 | `fmt.Println(LeastCommonMultiple(6, 8))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 8 | `fmt.Println(LeastCommonMultiple(12, 18))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 9 | `}` | نهاية الكتلة الحالية. |

## طريقة التشغيل

```bash
cd test/exercisestests/32_least_common_multiple_uint
go run .
```

## نصائح لفهم التمرين

- اكتب قيم المتغيرات على ورقة بعد كل دورة من الحلقة.
- فرّق بين index الذي يبدأ غالبًا من 0، وبين العد البشري الذي يبدأ من 1.
- اختبر المدخل الفارغ، والصفر، والقيم السالبة، وعنصرًا واحدًا عندما تكون مناسبة لنوع التمرين.
- لا تحفظ الناتج فقط؛ تتبع سبب تغير كل متغير.

## التعقيد

استخرج التعقيد من عدد الحلقات وحجم البيانات: حلقة واحدة غالبًا `O(n)`، حلقتان متداخلتان غالبًا `O(n²)`، والبحث الثنائي غالبًا `O(log n)`. راجع الكود الفعلي لأن بعض الحلقات تعمل على أبعاد مختلفة.
