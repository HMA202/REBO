# شرح التمرين `50_simple_calculator` — simple calculator

## فكرة المجلد

هذا تمرين تدريبي إضافي. ملف `exercise.go` يحتوي الحل أو الدوال الأساسية، وملف `main.go` يحتوي أمثلة تشغيل تساعد على رؤية النتيجة.

## الدوال الموجودة

- `SimpleCalculator(expr string) int`

## `exercise.go`

```go
package main

func SimpleCalculator(expr string) int {
	result := 0
	current := 0
	sign := 1
	hasNumber := false

	for i := 0; i < len(expr); i++ {
		ch := expr[i]

		if ch >= '0' && ch <= '9' {
			current = current*10 + int(ch-'0')
			hasNumber = true
		} else if ch == '+' || ch == '-' {
			if hasNumber {
				result += sign * current
			}

			current = 0
			hasNumber = false

			if ch == '-' {
				sign = -1
			} else {
				sign = 1
			}
		}
	}

	if hasNumber {
		result += sign * current
	}

	return result
}
```

### شرح كل سطر في الحل

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func SimpleCalculator(expr string) int {` | تعريف دالة `SimpleCalculator` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `result := 0` | إنشاء المتغير `result` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 5 | `current := 0` | إنشاء المتغير `current` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 6 | `sign := 1` | إنشاء المتغير `sign` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 7 | `hasNumber := false` | إنشاء المتغير `hasNumber` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 9 | `for i := 0; i < len(expr); i++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 10 | `ch := expr[i]` | إنشاء المتغير `ch` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 12 | `if ch >= '0' && ch <= '9' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 13 | `current = current*10 + int(ch-'0')` | إسناد أو تحديث قيمة `current` وفق التعبير الموجود في الجهة اليمنى. |
| 14 | `hasNumber = true` | إسناد أو تحديث قيمة `hasNumber` وفق التعبير الموجود في الجهة اليمنى. |
| 15 | `} else if ch == '+' \|\| ch == '-' {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 16 | `if hasNumber {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 17 | `result += sign * current` | إسناد أو تحديث قيمة `result +` وفق التعبير الموجود في الجهة اليمنى. |
| 18 | `}` | نهاية الكتلة الحالية. |
| 20 | `current = 0` | إسناد أو تحديث قيمة `current` وفق التعبير الموجود في الجهة اليمنى. |
| 21 | `hasNumber = false` | إسناد أو تحديث قيمة `hasNumber` وفق التعبير الموجود في الجهة اليمنى. |
| 23 | `if ch == '-' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 24 | `sign = -1` | إسناد أو تحديث قيمة `sign` وفق التعبير الموجود في الجهة اليمنى. |
| 25 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 26 | `sign = 1` | إسناد أو تحديث قيمة `sign` وفق التعبير الموجود في الجهة اليمنى. |
| 27 | `}` | نهاية الكتلة الحالية. |
| 28 | `}` | نهاية الكتلة الحالية. |
| 29 | `}` | نهاية الكتلة الحالية. |
| 31 | `if hasNumber {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 32 | `result += sign * current` | إسناد أو تحديث قيمة `result +` وفق التعبير الموجود في الجهة اليمنى. |
| 33 | `}` | نهاية الكتلة الحالية. |
| 35 | `return result` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 36 | `}` | نهاية الكتلة الحالية. |

## كيف يعمل الحل؟

- يستخدم حلقة `for` لتكرار الفحص أو المعالجة.
- يحتوي استدعاءات دوال؛ راقب هل يوجد recursion أو دوال مساعدة.
- قد يستخدم مؤشرات أو عمليات ضرب؛ يُحدد المعنى من السياق والتوقيع.

### تتبع عام للتنفيذ

1. يبدأ التنفيذ عند استدعاء الدالة الأساسية بالقيم المطلوبة.
2. تُنشأ المتغيرات المساعدة والقيم الابتدائية.
3. تنفذ الحلقات أو الشروط بالترتيب الظاهر في الكود.
4. عند تحقق حالة إرجاع مبكر تتوقف الدالة فورًا؛ وإلا تستمر حتى النتيجة النهائية.
5. القيمة المعادة أو التعديل النهائي يظهر في `main.go`.

## `main.go` المستخدم للتجربة

```go
package main

import "fmt"

func main() {
	fmt.Println(SimpleCalculator("10+5-3"))
	fmt.Println(SimpleCalculator("100-20-5"))
	fmt.Println(SimpleCalculator("7+8+9"))
}
```

### شرح أسطر ملف التجربة

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "fmt"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `fmt.Println(SimpleCalculator("10+5-3"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 7 | `fmt.Println(SimpleCalculator("100-20-5"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 8 | `fmt.Println(SimpleCalculator("7+8+9"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 9 | `}` | نهاية الكتلة الحالية. |

## طريقة التشغيل

```bash
cd test/exercisestests/50_simple_calculator
go run .
```

## نصائح لفهم التمرين

- اكتب قيم المتغيرات على ورقة بعد كل دورة من الحلقة.
- فرّق بين index الذي يبدأ غالبًا من 0، وبين العد البشري الذي يبدأ من 1.
- اختبر المدخل الفارغ، والصفر، والقيم السالبة، وعنصرًا واحدًا عندما تكون مناسبة لنوع التمرين.
- لا تحفظ الناتج فقط؛ تتبع سبب تغير كل متغير.

## التعقيد

استخرج التعقيد من عدد الحلقات وحجم البيانات: حلقة واحدة غالبًا `O(n)`، حلقتان متداخلتان غالبًا `O(n²)`، والبحث الثنائي غالبًا `O(log n)`. راجع الكود الفعلي لأن بعض الحلقات تعمل على أبعاد مختلفة.
