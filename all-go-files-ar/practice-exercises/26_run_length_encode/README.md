# شرح التمرين `26_run_length_encode` — run length encode

## فكرة المجلد

هذا تمرين تدريبي إضافي. ملف `exercise.go` يحتوي الحل أو الدوال الأساسية، وملف `main.go` يحتوي أمثلة تشغيل تساعد على رؤية النتيجة.

## الدوال الموجودة

- `IntToStringRLE(n int) string`
- `RunLengthEncode(s string) string`

## `exercise.go`

```go
package main

func IntToStringRLE(n int) string {
	if n == 0 {
		return "0"
	}

	result := ""

	for n > 0 {
		result = string(rune('0'+n%10)) + result
		n /= 10
	}

	return result
}

func RunLengthEncode(s string) string {
	result := ""
	i := 0

	for i < len(s) {
		if s[i] == ' ' {
			result += " "
			i++
			continue
		}

		count := 1

		for i+count < len(s) && s[i+count] == s[i] {
			count++
		}

		result += IntToStringRLE(count) + string(s[i])
		i += count
	}

	return result
}
```

### شرح كل سطر في الحل

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func IntToStringRLE(n int) string {` | تعريف دالة `IntToStringRLE` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `if n == 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 5 | `return "0"` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 6 | `}` | نهاية الكتلة الحالية. |
| 8 | `result := ""` | إنشاء المتغير `result` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 10 | `for n > 0 {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 11 | `result = string(rune('0'+n%10)) + result` | إسناد أو تحديث قيمة `result` وفق التعبير الموجود في الجهة اليمنى. |
| 12 | `n /= 10` | إسناد أو تحديث قيمة `n /` وفق التعبير الموجود في الجهة اليمنى. |
| 13 | `}` | نهاية الكتلة الحالية. |
| 15 | `return result` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 16 | `}` | نهاية الكتلة الحالية. |
| 18 | `func RunLengthEncode(s string) string {` | تعريف دالة `RunLengthEncode` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 19 | `result := ""` | إنشاء المتغير `result` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 20 | `i := 0` | إنشاء المتغير `i` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 22 | `for i < len(s) {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 23 | `if s[i] == ' ' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 24 | `result += " "` | إسناد أو تحديث قيمة `result +` وفق التعبير الموجود في الجهة اليمنى. |
| 25 | `i++` | يزيد قيمة العداد بمقدار 1. |
| 26 | `continue` | يتجاوز بقية أوامر الدورة الحالية وينتقل إلى الدورة التالية من الحلقة. |
| 27 | `}` | نهاية الكتلة الحالية. |
| 29 | `count := 1` | إنشاء المتغير `count` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 31 | `for i+count < len(s) && s[i+count] == s[i] {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 32 | `count++` | يزيد قيمة العداد بمقدار 1. |
| 33 | `}` | نهاية الكتلة الحالية. |
| 35 | `result += IntToStringRLE(count) + string(s[i])` | إسناد أو تحديث قيمة `result +` وفق التعبير الموجود في الجهة اليمنى. |
| 36 | `i += count` | إسناد أو تحديث قيمة `i +` وفق التعبير الموجود في الجهة اليمنى. |
| 37 | `}` | نهاية الكتلة الحالية. |
| 39 | `return result` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 40 | `}` | نهاية الكتلة الحالية. |

## كيف يعمل الحل؟

- يستخدم حلقة `for` لتكرار الفحص أو المعالجة.
- يحتوي استدعاءات دوال؛ راقب هل يوجد recursion أو دوال مساعدة.
- عامل `%` يُستخدم للباقي، وغالبًا لفحص القابلية للقسمة أو استخراج الخانات.
- قد يستخدم مؤشرات أو عمليات ضرب؛ يُحدد المعنى من السياق والتوقيع.

### تتبع عام للتنفيذ

1. يبدأ التنفيذ عند استدعاء الدالة الأساسية بالقيم المطلوبة.
2. تُنشأ المتغيرات المساعدة والقيم الابتدائية.
3. تنفذ الحلقات أو الشروط بالترتيب الظاهر في الكود.
4. عند تحقق حالة إرجاع مبكر تتوقف الدالة فورًا؛ وإلا تستمر حتى النتيجة النهائية.
5. القيمة المعادة أو التعديل النهائي يظهر في `main.go`.

## `main.go` المستخدم للتجربة

```go
package main

import "fmt"

func main() {
	fmt.Println(RunLengthEncode("i loooove you"))
	fmt.Println(RunLengthEncode("aaabbc"))
}
```

### شرح أسطر ملف التجربة

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "fmt"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `fmt.Println(RunLengthEncode("i loooove you"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 7 | `fmt.Println(RunLengthEncode("aaabbc"))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 8 | `}` | نهاية الكتلة الحالية. |

## طريقة التشغيل

```bash
cd test/exercisestests/26_run_length_encode
go run .
```

## نصائح لفهم التمرين

- اكتب قيم المتغيرات على ورقة بعد كل دورة من الحلقة.
- فرّق بين index الذي يبدأ غالبًا من 0، وبين العد البشري الذي يبدأ من 1.
- اختبر المدخل الفارغ، والصفر، والقيم السالبة، وعنصرًا واحدًا عندما تكون مناسبة لنوع التمرين.
- لا تحفظ الناتج فقط؛ تتبع سبب تغير كل متغير.

## التعقيد

استخرج التعقيد من عدد الحلقات وحجم البيانات: حلقة واحدة غالبًا `O(n)`، حلقتان متداخلتان غالبًا `O(n²)`، والبحث الثنائي غالبًا `O(log n)`. راجع الكود الفعلي لأن بعض الحلقات تعمل على أبعاد مختلفة.
