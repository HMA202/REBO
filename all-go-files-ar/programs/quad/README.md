# شرح مشروع `quad` كاملًا

هذا المشروع يحتوي برنامج اختبار وحزمة `piscine`. الحزمة ترسم خمسة أشكال Quad، وتحتوي أيضًا دالتي `CountChar` و`CountLetters`.

## بنية الملفات

- `main.go`
- `piscine/countchar.go`
- `piscine/countletters.go`
- `piscine/quad.go`

## كيف تعمل ملفات الحزمة معًا؟

- `main.go` يستورد `quad/piscine` ويستدعي الدوال للتجربة.
- `piscine/quad.go` يحتوي دالة السطر المشتركة ورسومات QuadA إلى QuadE.
- `piscine/countchar.go` يحسب تكرار rune مستهدفة.
- `piscine/countletters.go` يضغط الأحرف المتكررة المتجاورة ويطبع العدد والحرف.

## شرح خوارزمية الرسم

1. `printLine` مسؤولة عن سطر واحد: حرف يسار، حروف وسط، حرف يمين.
2. كل دالة Quad تمر على الصفوف من 1 إلى y.
3. الصف الأول والأخير يحصلان على رموز الحواف، والصفوف الوسطية على الحدود والمسافات.
4. الشرط `x <= 0 || y <= 0` يمنع أي طباعة للأبعاد غير الصالحة.
5. الحالات x=1 أو y=1 تعمل من خلال ترتيب شروط أول عمود/آخر عمود وأول صف/آخر صف.

## الملفات والكود

### `main.go`

```go
package main

import (
	"fmt"
	"quad/piscine"
)

func main() {
	fmt.Println(piscine.CountChar("i loooove you jjj", 'j'))
	piscine.CountLetters("i loooove you")
	piscine.QuadA(5, 3)
	piscine.QuadA(5, 1)
	piscine.QuadA(1, 1)
	piscine.QuadA(1, 5)
	piscine.QuadB(5, 3)
	piscine.QuadB(5, 1)
	piscine.QuadB(1, 1)
	piscine.QuadB(1, 5)
	piscine.QuadC(5, 3)
	piscine.QuadC(5, 1)
	piscine.QuadC(1, 1)
	piscine.QuadC(1, 5)
	piscine.QuadD(5, 3)
	piscine.QuadD(5, 1)
	piscine.QuadD(1, 1)
	piscine.QuadD(1, 5)
}
```

#### شرح الأسطر

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package main` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import (` | بداية مجموعة الاستيرادات التي يحتاج إليها الملف. |
| 4 | `"fmt"` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 5 | `"quad/piscine"` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 6 | `)` | نهاية كتلة الاستيراد أو الاستدعاء متعدد الأسطر حسب السياق. |
| 8 | `func main() {` | تعريف دالة `main` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 9 | `fmt.Println(piscine.CountChar("i loooove you jjj", 'j'))` | يطبع قيمة أو نصًا باستخدام حزمة `fmt`. |
| 10 | `piscine.CountLetters("i loooove you")` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 11 | `piscine.QuadA(5, 3)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 12 | `piscine.QuadA(5, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 13 | `piscine.QuadA(1, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 14 | `piscine.QuadA(1, 5)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 15 | `piscine.QuadB(5, 3)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 16 | `piscine.QuadB(5, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 17 | `piscine.QuadB(1, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 18 | `piscine.QuadB(1, 5)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 19 | `piscine.QuadC(5, 3)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 20 | `piscine.QuadC(5, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 21 | `piscine.QuadC(1, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 22 | `piscine.QuadC(1, 5)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 23 | `piscine.QuadD(5, 3)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 24 | `piscine.QuadD(5, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 25 | `piscine.QuadD(1, 1)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 26 | `piscine.QuadD(1, 5)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 27 | `}` | نهاية الكتلة الحالية. |

### `piscine/countchar.go`

```go
package piscine

func CountChar(s string, target rune) int {
	count := 0

	for _, ch := range s {
		if ch == target {
			count++
		}
	}

	return count
}
```

#### شرح الأسطر

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package piscine` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func CountChar(s string, target rune) int {` | تعريف دالة `CountChar` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `count := 0` | إنشاء المتغير `count` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 6 | `for _, ch := range s {` | حلقة `range` تمر على عناصر النص أو الـslice؛ المتغير `_` يعني تجاهل قيمة لا نحتاجها. |
| 7 | `if ch == target {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 8 | `count++` | يزيد قيمة العداد بمقدار 1. |
| 9 | `}` | نهاية الكتلة الحالية. |
| 10 | `}` | نهاية الكتلة الحالية. |
| 12 | `return count` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 13 | `}` | نهاية الكتلة الحالية. |

### `piscine/countletters.go`

```go
package piscine

import "github.com/01-edu/z01"

func printNumber(n int) {
	if n >= 10 {
		printNumber(n / 10)
	}
	z01.PrintRune('0' + rune(n%10))
}

func CountLetters(s string) {
	i := 0

	for i < len(s) {
		if s[i] == ' ' {
			z01.PrintRune(' ')
			i++
			continue
		}

		count := 1

		for i+count < len(s) && s[i+count] == s[i] {
			count++
		}

		printNumber(count)
		z01.PrintRune(rune(s[i]))

		i += count
	}

	z01.PrintRune('\n')
}
```

#### شرح الأسطر

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package piscine` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "github.com/01-edu/z01"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func printNumber(n int) {` | تعريف دالة `printNumber` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `if n >= 10 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 7 | `printNumber(n / 10)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 8 | `}` | نهاية الكتلة الحالية. |
| 9 | `z01.PrintRune('0' + rune(n%10))` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 10 | `}` | نهاية الكتلة الحالية. |
| 12 | `func CountLetters(s string) {` | تعريف دالة `CountLetters` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 13 | `i := 0` | إنشاء المتغير `i` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 15 | `for i < len(s) {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 16 | `if s[i] == ' ' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 17 | `z01.PrintRune(' ')` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 18 | `i++` | يزيد قيمة العداد بمقدار 1. |
| 19 | `continue` | يتجاوز بقية أوامر الدورة الحالية وينتقل إلى الدورة التالية من الحلقة. |
| 20 | `}` | نهاية الكتلة الحالية. |
| 22 | `count := 1` | إنشاء المتغير `count` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 24 | `for i+count < len(s) && s[i+count] == s[i] {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 25 | `count++` | يزيد قيمة العداد بمقدار 1. |
| 26 | `}` | نهاية الكتلة الحالية. |
| 28 | `printNumber(count)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 29 | `z01.PrintRune(rune(s[i]))` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 31 | `i += count` | إسناد أو تحديث قيمة `i +` وفق التعبير الموجود في الجهة اليمنى. |
| 32 | `}` | نهاية الكتلة الحالية. |
| 34 | `z01.PrintRune('\n')` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 35 | `}` | نهاية الكتلة الحالية. |

### `piscine/quad.go`

```go
package piscine

import "github.com/01-edu/z01"

func printRune(r rune) {
	z01.PrintRune(r)
}

func printLine(x int, left rune, middle rune, right rune) {
	for col := 1; col <= x; col++ {
		if col == 1 {
			printRune(left)
		} else if col == x {
			printRune(right)
		} else {
			printRune(middle)
		}
	}
	printRune('\n')
}

func QuadA(x, y int) {
	if x <= 0 || y <= 0 {
		return
	}

	for row := 1; row <= y; row++ {
		if row == 1 || row == y {
			printLine(x, 'o', '-', 'o')
		} else {
			printLine(x, '|', ' ', '|')
		}
	}
}

func QuadB(x, y int) {
	if x <= 0 || y <= 0 {
		return
	}

	for row := 1; row <= y; row++ {
		if row == 1 {
			printLine(x, '/', '*', '\\')
		} else if row == y {
			printLine(x, '\\', '*', '/')
		} else {
			printLine(x, '*', ' ', '*')
		}
	}
}

func QuadC(x, y int) {
	if x <= 0 || y <= 0 {
		return
	}

	for row := 1; row <= y; row++ {
		if row == 1 {
			printLine(x, 'A', 'B', 'A')
		} else if row == y {
			printLine(x, 'C', 'B', 'C')
		} else {
			printLine(x, 'B', ' ', 'B')
		}
	}
}

func QuadD(x, y int) {
	if x <= 0 || y <= 0 {
		return
	}

	for row := 1; row <= y; row++ {
		if row == 1 {
			printLine(x, 'A', 'B', 'C')
		} else if row == y {
			printLine(x, 'A', 'B', 'C')
		} else {
			printLine(x, 'B', ' ', 'B')
		}
	}
}

func QuadE(x, y int) {
	if x <= 0 || y <= 0 {
		return
	}

	for row := 1; row <= y; row++ {
		if row == 1 {
			printLine(x, 'A', 'B', 'C')
		} else if row == y {
			printLine(x, 'C', 'B', 'A')
		} else {
			printLine(x, 'B', ' ', 'B')
		}
	}
}
```

#### شرح الأسطر

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package piscine` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "github.com/01-edu/z01"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func printRune(r rune) {` | تعريف دالة `printRune` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `z01.PrintRune(r)` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 7 | `}` | نهاية الكتلة الحالية. |
| 9 | `func printLine(x int, left rune, middle rune, right rune) {` | تعريف دالة `printLine` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 10 | `for col := 1; col <= x; col++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 11 | `if col == 1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 12 | `printRune(left)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 13 | `} else if col == x {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 14 | `printRune(right)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 15 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 16 | `printRune(middle)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 17 | `}` | نهاية الكتلة الحالية. |
| 18 | `}` | نهاية الكتلة الحالية. |
| 19 | `printRune('\n')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 20 | `}` | نهاية الكتلة الحالية. |
| 22 | `func QuadA(x, y int) {` | تعريف دالة `QuadA` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 23 | `if x <= 0 \|\| y <= 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 24 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 25 | `}` | نهاية الكتلة الحالية. |
| 27 | `for row := 1; row <= y; row++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 28 | `if row == 1 \|\| row == y {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 29 | `printLine(x, 'o', '-', 'o')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 30 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 31 | `printLine(x, '\|', ' ', '\|')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 32 | `}` | نهاية الكتلة الحالية. |
| 33 | `}` | نهاية الكتلة الحالية. |
| 34 | `}` | نهاية الكتلة الحالية. |
| 36 | `func QuadB(x, y int) {` | تعريف دالة `QuadB` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 37 | `if x <= 0 \|\| y <= 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 38 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 39 | `}` | نهاية الكتلة الحالية. |
| 41 | `for row := 1; row <= y; row++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 42 | `if row == 1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 43 | `printLine(x, '/', '*', '\\')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 44 | `} else if row == y {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 45 | `printLine(x, '\\', '*', '/')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 46 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 47 | `printLine(x, '*', ' ', '*')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 48 | `}` | نهاية الكتلة الحالية. |
| 49 | `}` | نهاية الكتلة الحالية. |
| 50 | `}` | نهاية الكتلة الحالية. |
| 52 | `func QuadC(x, y int) {` | تعريف دالة `QuadC` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 53 | `if x <= 0 \|\| y <= 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 54 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 55 | `}` | نهاية الكتلة الحالية. |
| 57 | `for row := 1; row <= y; row++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 58 | `if row == 1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 59 | `printLine(x, 'A', 'B', 'A')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 60 | `} else if row == y {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 61 | `printLine(x, 'C', 'B', 'C')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 62 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 63 | `printLine(x, 'B', ' ', 'B')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 64 | `}` | نهاية الكتلة الحالية. |
| 65 | `}` | نهاية الكتلة الحالية. |
| 66 | `}` | نهاية الكتلة الحالية. |
| 68 | `func QuadD(x, y int) {` | تعريف دالة `QuadD` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 69 | `if x <= 0 \|\| y <= 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 70 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 71 | `}` | نهاية الكتلة الحالية. |
| 73 | `for row := 1; row <= y; row++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 74 | `if row == 1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 75 | `printLine(x, 'A', 'B', 'C')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 76 | `} else if row == y {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 77 | `printLine(x, 'A', 'B', 'C')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 78 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 79 | `printLine(x, 'B', ' ', 'B')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 80 | `}` | نهاية الكتلة الحالية. |
| 81 | `}` | نهاية الكتلة الحالية. |
| 82 | `}` | نهاية الكتلة الحالية. |
| 84 | `func QuadE(x, y int) {` | تعريف دالة `QuadE` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 85 | `if x <= 0 \|\| y <= 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 86 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 87 | `}` | نهاية الكتلة الحالية. |
| 89 | `for row := 1; row <= y; row++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 90 | `if row == 1 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 91 | `printLine(x, 'A', 'B', 'C')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 92 | `} else if row == y {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 93 | `printLine(x, 'C', 'B', 'A')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 94 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 95 | `printLine(x, 'B', ' ', 'B')` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 96 | `}` | نهاية الكتلة الحالية. |
| 97 | `}` | نهاية الكتلة الحالية. |
| 98 | `}` | نهاية الكتلة الحالية. |

## أمثلة الرسم

```text
QuadA(5,3)
o---o
|   |
o---o

QuadB(5,3)
/***\
*   *
\***/
```

## أخطاء شائعة

- كتابة `'\'` خطأ؛ لطباعة backslash كـ rune نكتب `'\\'`.
- عدم طباعة newline بعد كل سطر يجعل المستطيل كله في سطر واحد.
- خلط `package main` و`package piscine` داخل المجلد نفسه يسبب خطأ؛ لذلك ملفات الحزمة داخل `piscine/`.
