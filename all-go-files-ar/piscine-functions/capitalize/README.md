# شرح `capitalize.go` — تكبير أول حرف من كل كلمة وتصغير البقية

> الدالة الأساسية: `Capitalize`

## ما وظيفة الكود؟

تعتبر الكلمة سلسلة من الأحرف أو الأرقام. تكبر أول حرف إن كان صغيرًا، وتصغر الحروف الكبيرة اللاحقة، وتبدأ كلمة جديدة بعد أي محرف غير أبجدي رقمي.

## الكود الأصلي

```go
package piscine

func Capitalize(s string) string {
	result := ""
	startOfWord := true

	for _, r := range s {
		isLower := r >= 'a' && r <= 'z'
		isUpper := r >= 'A' && r <= 'Z'
		isDigit := r >= '0' && r <= '9'
		isAlphaNumeric := isLower || isUpper || isDigit

		if isAlphaNumeric {
			if startOfWord {
				if isLower {
					r = r - ('a' - 'A')
				}

				startOfWord = false
			} else if isUpper {
				r = r + ('a' - 'A')
			}
		} else {
			startOfWord = true
		}

		result += string(r)
	}

	return result
}
```

## المدخلات والمخرجات

```go
func Capitalize(s string) string
```

الدالة تستقبل القيم حسب التوقيع أعلاه، ثم إما تعيد نتيجة بواسطة `return` أو تعدّل قيمة عبر pointer أو تطبع باستخدام `z01.PrintRune`.

## تدفق التنفيذ خطوة بخطوة

1. استخدام startOfWord لمعرفة هل المحرف الحالي في بداية كلمة.
2. تحديد هل المحرف حرف صغير أو كبير أو رقم.
3. إذا كان أول حرف في كلمة وكان صغيرًا، تحويله إلى كبير.
4. إذا كان داخل الكلمة وكان كبيرًا، تحويله إلى صغير.
5. أي فاصل يعيد startOfWord إلى true.

## تتبع بأمثلة

- `Capitalize("hello! HOW are you?")` ⟶ `"Hello! How Are You?"`
- `Capitalize("42mIXed")` ⟶ `"42mixed"`

## المفاهيم المهمة

- **آلة حالات بسيطة**
- **مجالات ASCII**
- **تحويل حالة الحرف بفارق a-A**

## شرح كل سطر

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package piscine` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `func Capitalize(s string) string {` | تعريف دالة `Capitalize` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 4 | `result := ""` | إنشاء المتغير `result` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 5 | `startOfWord := true` | إنشاء المتغير `startOfWord` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 7 | `for _, r := range s {` | حلقة `range` تمر على عناصر النص أو الـslice؛ المتغير `_` يعني تجاهل قيمة لا نحتاجها. |
| 8 | `isLower := r >= 'a' && r <= 'z'` | إنشاء المتغير `isLower` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 9 | `isUpper := r >= 'A' && r <= 'Z'` | إنشاء المتغير `isUpper` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 10 | `isDigit := r >= '0' && r <= '9'` | إنشاء المتغير `isDigit` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 11 | `isAlphaNumeric := isLower \|\| isUpper \|\| isDigit` | إنشاء المتغير `isAlphaNumeric` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 13 | `if isAlphaNumeric {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 14 | `if startOfWord {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 15 | `if isLower {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 16 | `r = r - ('a' - 'A')` | إسناد أو تحديث قيمة `r` وفق التعبير الموجود في الجهة اليمنى. |
| 17 | `}` | نهاية الكتلة الحالية. |
| 19 | `startOfWord = false` | إسناد أو تحديث قيمة `startOfWord` وفق التعبير الموجود في الجهة اليمنى. |
| 20 | `} else if isUpper {` | حالة بديلة مشروطة؛ تُفحص فقط إذا لم يتحقق الشرط السابق. |
| 21 | `r = r + ('a' - 'A')` | إسناد أو تحديث قيمة `r` وفق التعبير الموجود في الجهة اليمنى. |
| 22 | `}` | نهاية الكتلة الحالية. |
| 23 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 24 | `startOfWord = true` | إسناد أو تحديث قيمة `startOfWord` وفق التعبير الموجود في الجهة اليمنى. |
| 25 | `}` | نهاية الكتلة الحالية. |
| 27 | `result += string(r)` | إسناد أو تحديث قيمة `result +` وفق التعبير الموجود في الجهة اليمنى. |
| 28 | `}` | نهاية الكتلة الحالية. |
| 30 | `return result` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 31 | `}` | نهاية الكتلة الحالية. |

## الحالات الخاصة والأخطاء الشائعة

- الأرقام تُعد جزءًا من الكلمة.
- الفواصل والرموز تبقى كما هي.
- الدالة تتعامل مع الحروف الإنجليزية فقط.

## التعقيد

الزمن O(n)، وبناء result باستخدام += قد يتطلب نسخًا متكررًا؛ الذاكرة O(n).

## طريقة تجربة الدالة

لأن الملف يستخدم `package piscine` فهو ليس برنامجًا مستقلًا. أنشئ ملف `main.go` مؤقتًا في مجلد آخر يستورد الحزمة، أو استخدم ملف اختبار Go. مثال اختبار مبسط:

```go
package piscine

import "testing"

func TestCapitalize(t *testing.T) {
	// ضع هنا حالات الاختبار المناسبة كما في الأمثلة أعلاه.
}
```

> لا تضف ملف `main.go` داخل مجلد التسليم إذا كانت منصة التمرين تطلب ملف الدالة فقط.
