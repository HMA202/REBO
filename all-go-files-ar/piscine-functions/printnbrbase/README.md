# شرح `printnbrbase.go` — طباعة عدد بقاعدة مخصصة

> الدالة الأساسية: `PrintNbrBase`

## ما وظيفة الكود؟

تطبع nbr باستخدام الرموز الموجودة في base. تطبع NV إذا كانت القاعدة غير صالحة، وتدعم السالب وأصغر int عبر إبقاء القيمة سالبة أثناء التحويل.

## الكود الأصلي

```go
package piscine

import "github.com/01-edu/z01"

func PrintNbrBase(nbr int, base string) {
	baseRunes := []rune(base)

	if !validPrintNbrBase(baseRunes) {
		z01.PrintRune('N')
		z01.PrintRune('V')
		return
	}

	if nbr == 0 {
		z01.PrintRune(baseRunes[0])
		return
	}

	if nbr < 0 {
		z01.PrintRune('-')
	} else {
		nbr = -nbr
	}

	printNegativeNbrBase(nbr, baseRunes)
}

func validPrintNbrBase(base []rune) bool {
	if len(base) < 2 {
		return false
	}

	for i := 0; i < len(base); i++ {
		if base[i] == '+' || base[i] == '-' {
			return false
		}

		for j := i + 1; j < len(base); j++ {
			if base[i] == base[j] {
				return false
			}
		}
	}

	return true
}

func printNegativeNbrBase(nbr int, base []rune) {
	baseLength := len(base)

	if nbr <= -baseLength {
		printNegativeNbrBase(nbr/baseLength, base)
	}

	index := -(nbr % baseLength)
	z01.PrintRune(base[index])
}
```

## المدخلات والمخرجات

```go
func PrintNbrBase(nbr int, base string)
```

```go
func validPrintNbrBase(base []rune) bool
```

```go
func printNegativeNbrBase(nbr int, base []rune)
```

الدالة تستقبل القيم حسب التوقيع أعلاه، ثم إما تعيد نتيجة بواسطة `return` أو تعدّل قيمة عبر pointer أو تطبع باستخدام `z01.PrintRune`.

## تدفق التنفيذ خطوة بخطوة

1. تحويل base إلى []rune والتحقق منها.
2. طباعة رمز الصفر إذا nbr==0.
3. طباعة - للسالب، أو تحويل الموجب إلى سالب.
4. الاستدعاء recursive يقسم على طول القاعدة ليطبع الخانات الأعلى أولًا.
5. استخدام باقي القسمة السالب وتحويله إلى index موجب.

## تتبع بأمثلة

- `PrintNbrBase(15, "01")` ⟶ `1111`
- `PrintNbrBase(255, "0123456789ABCDEF")` ⟶ `FF`
- `قاعدة غير صالحة` ⟶ `NV`

## المفاهيم المهمة

- **أنظمة العد**
- **recursion**
- **التحقق من القاعدة**
- **معالجة min int**

## شرح كل سطر

| السطر | التعليمة | الشرح |
|---:|---|---|
| 1 | `package piscine` | يحدد الحزمة التي ينتمي إليها الملف؛ `piscine` تعني أن الدالة جزء من مكتبة التمارين، و`main` تعني برنامجًا قابلًا للتشغيل. |
| 3 | `import "github.com/01-edu/z01"` | يستورد الحزمة المذكورة حتى يمكن استخدام دوالها أو قيمها داخل الملف. |
| 5 | `func PrintNbrBase(nbr int, base string) {` | تعريف دالة `PrintNbrBase` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 6 | `baseRunes := []rune(base)` | إنشاء المتغير `baseRunes` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 8 | `if !validPrintNbrBase(baseRunes) {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 9 | `z01.PrintRune('N')` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 10 | `z01.PrintRune('V')` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 11 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 12 | `}` | نهاية الكتلة الحالية. |
| 14 | `if nbr == 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 15 | `z01.PrintRune(baseRunes[0])` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 16 | `return` | تنهي تنفيذ الدالة فورًا من دون إرجاع قيمة. |
| 17 | `}` | نهاية الكتلة الحالية. |
| 19 | `if nbr < 0 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 20 | `z01.PrintRune('-')` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 21 | `} else {` | الحالة البديلة التي تعمل عندما لا يتحقق الشرط السابق. |
| 22 | `nbr = -nbr` | إسناد أو تحديث قيمة `nbr` وفق التعبير الموجود في الجهة اليمنى. |
| 23 | `}` | نهاية الكتلة الحالية. |
| 25 | `printNegativeNbrBase(nbr, baseRunes)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 26 | `}` | نهاية الكتلة الحالية. |
| 28 | `func validPrintNbrBase(base []rune) bool {` | تعريف دالة `validPrintNbrBase` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 29 | `if len(base) < 2 {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 30 | `return false` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 31 | `}` | نهاية الكتلة الحالية. |
| 33 | `for i := 0; i < len(base); i++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 34 | `if base[i] == '+' \|\| base[i] == '-' {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 35 | `return false` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 36 | `}` | نهاية الكتلة الحالية. |
| 38 | `for j := i + 1; j < len(base); j++ {` | حلقة `for` بعداد أو شرط؛ تتحكم في تكرار الأوامر الموجودة داخلها. |
| 39 | `if base[i] == base[j] {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 40 | `return false` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 41 | `}` | نهاية الكتلة الحالية. |
| 42 | `}` | نهاية الكتلة الحالية. |
| 43 | `}` | نهاية الكتلة الحالية. |
| 45 | `return true` | تنهي الدالة فورًا وتعيد القيمة أو التعبير المذكور إلى المستدعي. |
| 46 | `}` | نهاية الكتلة الحالية. |
| 48 | `func printNegativeNbrBase(nbr int, base []rune) {` | تعريف دالة `printNegativeNbrBase` مع المعاملات ونوع الإرجاع الظاهرين في التوقيع. |
| 49 | `baseLength := len(base)` | إنشاء المتغير `baseLength` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 51 | `if nbr <= -baseLength {` | شرط `if`: ينفذ الكتلة التالية فقط عندما يكون التعبير المنطقي صحيحًا. |
| 52 | `printNegativeNbrBase(nbr/baseLength, base)` | تعليمة Go ضمن الخوارزمية؛ معناها التفصيلي يتضح من قسم تدفق التنفيذ أعلاه ومن المتغيرات المحيطة بها. |
| 53 | `}` | نهاية الكتلة الحالية. |
| 55 | `index := -(nbr % baseLength)` | إنشاء المتغير `index` لأول مرة واستنتاج نوعه تلقائيًا من القيمة الموجودة يمين `:=`. |
| 56 | `z01.PrintRune(base[index])` | يطبع rune واحدة باستخدام الدالة المسموحة في تمارين piscine. |
| 57 | `}` | نهاية الكتلة الحالية. |

## الحالات الخاصة والأخطاء الشائعة

- القاعدة يجب أن تحتوي رمزين فريدين على الأقل.
- لا تسمح بعلامتي + و-.
- لا تطبع newline تلقائيًا.

## التعقيد

التحقق O(b²)، والطباعة O(log_b |n|).

## طريقة تجربة الدالة

لأن الملف يستخدم `package piscine` فهو ليس برنامجًا مستقلًا. أنشئ ملف `main.go` مؤقتًا في مجلد آخر يستورد الحزمة، أو استخدم ملف اختبار Go. مثال اختبار مبسط:

```go
package piscine

import "testing"

func TestPrintNbrBase(t *testing.T) {
	// ضع هنا حالات الاختبار المناسبة كما في الأمثلة أعلاه.
}
```

> لا تضف ملف `main.go` داخل مجلد التسليم إذا كانت منصة التمرين تطلب ملف الدالة فقط.
